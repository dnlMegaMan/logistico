# Nivel de logueo. Da lo mismo si es en mayusculas o minusculas
# Valores posibles: ERROR | WARN | INFO | DEBUG | TRACE
LOGISTICO_NIVEL_LOGUEO=INFO

# Indica que ambiente (base de datos) usar al momento de truncar las tablas
# Valores posibles: PRODUCCION | TESTING | DESARROLLO
LOGISTICO_AMBIENTE_TRUNCADOR=