import { MoviminetoControlMinimo } from './movimineto-control-minimo';

describe('MoviminetoControlMinimo', () => {
  it('should create an instance', () => {
    expect(new MoviminetoControlMinimo()).toBeTruthy();
  });
});
