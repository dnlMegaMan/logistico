import { EstructuraListaUsuarios } from './estructura-lista-usuarios';

describe('EstructuraListaUsuarios', () => {
  it('should create an instance', () => {
    expect(new EstructuraListaUsuarios()).toBeTruthy();
  });
});
