import { ServicioUnidadBodegas } from './servicio-unidad-bodegas';

describe('ServicioUnidadBodegas', () => {
  it('should create an instance', () => {
    expect(new ServicioUnidadBodegas()).toBeTruthy();
  });
});
