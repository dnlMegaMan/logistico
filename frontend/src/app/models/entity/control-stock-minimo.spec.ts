import { ControlStockMinimo } from './control-stock-minimo';

describe('ControlStockMinimo', () => {
  it('should create an instance', () => {
    expect(new ControlStockMinimo()).toBeTruthy();
  });
});
