import { ServicioUnidadLogistico } from './servicio-unidad-logistico';

describe('ServicioUnidadLogistico', () => {
  it('should create an instance', () => {
    expect(new ServicioUnidadLogistico()).toBeTruthy();
  });
});
