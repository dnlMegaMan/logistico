import { DetallePlantillaConsumo } from './detalle-plantilla-consumo';

describe('DetallePlantillaConsumo', () => {
  it('should create an instance', () => {
    expect(new DetallePlantillaConsumo()).toBeTruthy();
  });
});
