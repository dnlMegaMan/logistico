import { EstructuraUsuarios } from './estructura-usuarios';

describe('EstructuraUsuarios', () => {
  it('should create an instance', () => {
    expect(new EstructuraUsuarios()).toBeTruthy();
  });
});
