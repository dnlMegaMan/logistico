import { DISPENSACIONRECETAS } from './dispensacion-recetas';

describe('DISPENSACIONRECETAS', () => {
  it('should create an instance', () => {
    expect(new DISPENSACIONRECETAS()).toBeTruthy();
  });
});
